body, html
  margin 0
  padding 0

*
  box-sizing border-box

input
  font-size: 100%;
  line-height: 1.15;
  margin: 0;
  outline none
  border none
  font inherit

/**
 * Show the overflow in IE.
 * 1. Show the overflow in Edge.
 */

button,
input { /* 1 */
  overflow: visible;
}
